#!/usr/bin/with-contenv bashio
set -euo pipefail

# shellcheck disable=SC1091
. /run/openwisp/env

cd /opt/openwisp

exec celery -A openwisp beat --loglevel="${DJANGO_LOG_LEVEL}" --schedule /data/celerybeat-schedule
